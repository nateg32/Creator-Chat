# Vercel Compatibility Wrapper

This folder exists for an older Vercel project whose root directory is configured as `frontend/anti-gravity`.

The real Creator Chat frontend lives in `frontend/creator-chat`.

New deployments should point Vercel, Netlify, or any static host directly at `frontend/creator-chat`. You can ignore this folder unless you are maintaining the existing compatibility deployment.
